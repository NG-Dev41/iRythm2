import { JoinWithCommaPipe } from './join-with-comma.pipe';

describe('JoinWithCommaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new JoinWithCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
